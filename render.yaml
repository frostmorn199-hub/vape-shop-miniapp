services:
  - type: web
    name: vape-shop-miniapp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: GOOGLE_CREDS_JSON
        sync: false
